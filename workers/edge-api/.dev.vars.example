# Copy to .dev.vars for local `pywrangler dev` / `wrangler dev` (never commit .dev.vars).
# Base URL of the real DocTranslater HTTP API, without trailing /v1/jobs
DOCTRANSLATE_UPSTREAM_URL=https://your-runtime-api.example.com
